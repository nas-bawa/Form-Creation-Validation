# Form-Creation-Validation
# User Registration Form with Validation

A simple registration form built with **HTML**, **CSS**, and **JavaScript**.  
This project demonstrates **basic client-side form validation** using string methods and conditional logic.

---

## 🚀 Features
- Clean and responsive form layout styled with CSS.
- Validation rules:
  - **Username** must be at least 3 characters long.
  - **Email** must include `@` and `.` characters.
  - **Password** must be at least 8 characters long.
- Feedback messages:
  - ✅ Success message in green when all validations pass.
  - ❌ Error messages in red when validations fail.
- Prevents form submission until inputs are valid.

---

## 📂 Project Structure
